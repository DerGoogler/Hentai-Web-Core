declare const config: {
    base: {
        title: string;
        swipeable: boolean;
    };
    options: {
        userAgent: string;
        userAgentWindows: string;
    };
};
export default config;

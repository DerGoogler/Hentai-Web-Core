import { hot } from "react-hot-loader/root";

const config = {
  base: {
    title: "Hentai Web",
    swipeable: true,
  },
  options: {
    userAgent: "HENTAI_WEB_AGENT",
  },
};

export default hot(config);


